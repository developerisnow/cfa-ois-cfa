# Copy to .env.secrets and customize for VPS demo
POSTGRES_DB=ois
POSTGRES_USER=ois
POSTGRES_PASSWORD=$(openssl rand -hex 16 2>/dev/null || echo ois_dev_password)
KC_ADMIN=admin
KC_ADMIN_PASSWORD=$(openssl rand -hex 12 2>/dev/null || echo admin)
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=$(openssl rand -hex 12 2>/dev/null || echo minioadmin)
