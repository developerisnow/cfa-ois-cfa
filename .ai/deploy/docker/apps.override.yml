version: "3.9"
services:
  api-gateway:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_GATEWAY:-58080}:8080"
  identity:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_IDENTITY:-56100}:8080"
  registry:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_REGISTRY:-56110}:8080"
  issuance:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_ISSUANCE:-56120}:8080"
  compliance:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_COMPLIANCE:-56130}:8080"
  settlement:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_SETTLEMENT:-56140}:8080"
  fabric-gateway:
    ports:
      - "${BIND_ADDR:-127.0.0.1}:${PORT_FABRICGW:-56150}:8080"
