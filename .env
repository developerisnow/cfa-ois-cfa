# Local dev env for OIS-CFA (non-secure)

# Infra host ports (non-standard to avoid conflicts)
POSTGRES_HOST_PORT=55432
KAFKA_HOST_PORT=59092
ZOOKEEPER_HOST_PORT=52181
KEYCLOAK_HOST_PORT=58080
MINIO_HOST_PORT=59000
MINIO_CONSOLE_PORT=59001

# Infra creds
POSTGRES_DB=ois
POSTGRES_USER=ois
POSTGRES_PASSWORD=ois_dev_password

KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin123

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin

# Service host ports
GATEWAY_HOST_PORT=55000
IDENTITY_HOST_PORT=55001
ESIA_HOST_PORT=55002
BANK_NOMINAL_HOST_PORT=55003
EDO_HOST_PORT=55004
ISSUANCE_HOST_PORT=55005
REGISTRY_HOST_PORT=55006
SETTLEMENT_HOST_PORT=55007
COMPLIANCE_HOST_PORT=55008

# Common connection strings (used by services via env)
SERVICE_DB_CONN=Host=postgres;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
KAFKA_BOOTSTRAP=kafka:9092

